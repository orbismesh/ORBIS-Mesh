[Match]
Name=br0

[Network]
# EUD / Mesh LAN
Address=@NODE_IP@/@NODE_CIDR@

# Management / SSH / Uplink
Address=@SSH_IP@/@SSH_CIDR@

# Default Route to Router-Net
Gateway=@GATEWAY@

DNS=@DNS@

IPForward=yes
IPMasquerade=ipv4

DHCP=no
LinkLocalAddressing=no
IPv6AcceptRA=no
