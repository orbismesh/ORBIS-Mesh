[Unit]
# Only start this per-interface unit if the kernel interface exists.
# Prevents long waits for sys-subsystem-net-devices-wlan2.device when the
# hardware is not present.
ConditionPathExists=/sys/class/net/wlan2
